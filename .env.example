# Runtime backend secrets only. Copy to .env and keep mode 600.

# Required only when [llm].provider = "openai_compatible".
POLYVERSE_LLM_API_KEY=

# Optional bearer token for local platform-gate requests.
# If set here, set the same value in the selected gate's private .env.
POLYVERSE_RUNTIME_GATE_TOKEN=
